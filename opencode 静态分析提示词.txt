===
"分析脚本{}是否存在将认证凭据（比如密码，token，apikey）以明文的方式写入文件（加密后的不算），不考虑其他脚本是否存在问题；禁止分析引用的其他脚本；如果存在问题将分析结果追加到result.txt文件中，没有问题不需要追加；特殊情况说明：echo $password|script.sh --这种形式的echo不是进程参数不存在参数记录历史记录的问题，除非是/bin/echo"

===
帮我分析文件{}如果涉及到用户名，帮我提取出这个名字，脚本必须写了明确的用户名称的才提取，不要去猜测。
将分析结果以追加的方式追加到users_result.txt文件(使用命令 echo 分析结果 >> users_java_result.txt)，未识别到用户名，不需要追加
忽略场景：
1、注释代码中的作者，如：@author zhangsan

===
你根据入口函数自上而下的分析可能存在的安全风险。特别关注输入参数导致的注入类问题，当你发现它有注入的风险时，要进一步根据调用链分析是否做了充分的校验，如果校验充分就不存在安全风险。

待分析的入口函数是：DeviceController.java文件的changeDevicePassword函数

要求：
1、要尽可能找到每个被调用函数的定义，这样才能分析充分，但是只在当前微服务内查找
2、因为分析的任务比较多，你要能自动规划子任务
3、如果上下文窗口过多，可以适当自动压缩上下文提示词
4、要求串行分析独立微服务，但是你也可以整体初步分析，把可能高风险的排在前面
5、你可以根据接口名称以及接口描述等信息初步判断它是否高风险，如果高风险先分析
6、分析结果统一放到当前目录的reports目录下
7、不管接口有没有安全风险都要做记录，没有安全风险的要记录为什么没有风险，比如，有参数做路劲拼接，但是做了参数校验所以没有风险等等
8、你是经验老到的安全专家，能通过目录名、文件名、类名、函数名等信息也能做一些初步的风险推测
9、如果通过代码发现涉及其他文件，如果需要，可以尝试在当前项目查找查看该文件，进一步确认安全风险。（在代码在可能写的是完整的路径，当前项目完整路径不一定能找到，可以找相对路径或者是文件名）

说明：
1、所有环境变量不要认为是外部可控，它都是写死的，不存在安全风险
2、还要重点关注密码认证凭据这些信息记录日志的风险
3、系统不存在json反序列化的问题，忽略该类型风险
4、没有实际风险的情况下，可以不进行参数校验
5、对于SQL注入，如果需要检查xml配置文件，你自动检查，检查有问题才报漏洞，否则不报
6、没有安全风险的建议项也不要报出来
7、记录的日志必须确认是密码认证凭据才报，不要一看到记录日志就报风险
8、对于DOS类型的要报那些确定下的漏洞，不要有一点怀疑就报
9、不要给我一些无意义的建议，我希望报的是确定的安全问题
10、忽略那些不会导致安全风险的安全编码问题
11、当你发现可能存在安全风险时，一定要尽可能深入从代码层面确认，直到没有更多的代码信息为止，因为你要保证不是误报
12、要确保外部输入的每一个参数都做了详细深入的分析，千万不要遗漏
13、对于校验函数，你要重点分析校验逻辑是否充分，不能因为校验就说无风险
14、如果是关键的输入参数，希望能自动开启一个独立的subagent任务，针对这个参数和特定的模式进行专门的独立深入分析
15、为了让分析尽可快和准确，你要自己规划出多个子任务来独立分析

===
我想分析整个chm格式的产品文档，按安全测试角度总结出安全测试建议和安全测试策略，但是文档的章节太多了，我希望根据章节目录结构把相关的章节作为一个独立的任务分析（章节必须是连续的，不能跳跃）。
@chm_extract/toc_hierarchy.json文件是章节目录，请先帮我按章节划分任务。现在开始分析章节“数据开发”及它下面的子章节。
只分析我给定的模式。

分析要求：
- 必须是读取分析读取章节的内容，禁用通过搜索关键字的方式
- 多个章节如果存在重复的部分，需要合并起来
- 总结要包含风险和对应的业务并包含理由

分析模式（与如下描述相似的场景都是符合的）：
- 文件是否提到将认证凭据写入文件--安全风险：认证凭据泄露
- 上传/导入文件--安全风险：如上传csv，需要关注csv注入、上传zip需要关注压缩炸弹等
- 要求用户设置密码--安全风险：后台可能将密码写入日志，关注敏感信息泄露风险
- 与第三方系统对接操作--与第三方对接属于高危风险
- 允许用户指定或上传可执行的文件（例如脚本、jar包等）--安全风险：执行恶意代码
- 允许用户进行二次编程，比如写可执行的代码，如python或者自定义的编程语言--安全风险：执行恶意代码
- 其他未知可能导致任意代码执行的高危风险业务

===
@ripgrep_output.json 文件是通过ripgrep搜索与命令执行相关的关键字结果文件，但是有些关键字可能并不跟命令执行相关，是误报。
我希望过滤掉不跟命令执行相关的，只保留命令执行相关的（看起来会执行命令就可以了，并不是一定要调用java的执行命令函数）。
与命令相关的概率分：高、中、低。概率为高的直接报告它是命令执行，概率为低的直接忽略，概率为中的，需要读文件。
一个文件只读一次，因此读的时候你要把需要确认得到条目一起告诉它。不相关的任务尽量用subagent分析

CommandExecutor.java调用了ProcessBuilder.start执行命令，存在安全风险，请根据数据流和函数调用链分析。
只分析命令注入（包括参数注入，也包括执行的命令或者脚本结合传递的参数综合来看的风险），凡是识别到有外部输入用于敏感操作的都存在安全风险，都要报告。
如果代码读写其他文件或者执行脚本，可能需要在项目内查找文件读取内容进一步分析。
为了避免误报你要分析source点到sink点直接从，参数是否做了充分的校验

===
分析src/main/java/com/weihua/controller/DeviceController.java文件的changeDevicePassword函数的source点参数传播到那些敏感操作，
并且识别出调用敏感操作前，做了哪些校验（比如通过正则限制只能是数字）。要根据数据流的分析方法，一直分析都找到不被调函数的定义为止。
敏感操作包括：文件操作（必须涉及到路径的）、命令执行、网络操作（比如将数据发送出去）。

要求：
- 不要输出任何安全风险和建议
- 禁止直接搜索敏感操作的关键字，一定要使用数据流自顶向下的方法进行分析
- 如果代码读写其他文件或者执行脚本，要查找文件读取内容进一步分析Sink点
 （文件路径不要求完全一致，但文件名必须一致。如果有多个一般选取匹配度最高的。并且必须在当前项目目录内查找）。如果source点会作为脚本的参数、或者写入文件。输出要体现
- sink点无敏感操作（比如用于记录日志），不体现在输出报告里。
- 重要：不要读写执行当前目录之外的文件 
- 输出保存在当前目录的reports.md文件中 
- 如果source参数包含多个字段的参数，可以考虑分字段体现，比如arg.name
- 忽略非空的校验，也就是非空校验不认为是校验，不体现

输出报告格式：                                                                                                                                                                                                                                                                                                                          
  路径 #1                
  【Source - 数据来源】
  文件: src/main/java/com/example/controller/FileController.java:45
  类: FileController
  方法: download(HttpServletRequest)
  参数: @RequestParam String fileName
  来源: 请求查询参数 "fileName"

  【Sink - 数据去向】
  文件: src/main/java/com/example/service/FileService.java:18
  类: FileService
  方法: readFile(String fileName)
  操作: Files.readString(Paths.get(getUploadDir(), fileName))
  参数: fileName (原始值)

  【校验逻辑】
  文件: src/main/java/com/example/controller/FileController.java:45

  代码: @Pattern(regexp = "^[a-zA-Z0-9_.-]+$", message = "文件名格式错误")
  描述: 仅允许字母、数字、点、下划线、短横线
  类型: 正则校验
  允许的特殊字符: _ . -
===
你是java数据流分析专家，分析给定的入口函数的source点参数传播到那些敏感操作，
并且识别出调用敏感操作前，做了哪些校验（比如通过正则限制只能是数字）。要根据数据流的分析方法，一直分析都找到不被调函数的定义为止。
敏感操作包括：文件操作（必须涉及到路径的）、命令执行、网络操作等（比如将数据发送出去）。

待分析文件：@src/main/java/com/weihua/controller/DeviceController.java 函数：changeDevicePassword

要求：
- 不要输出任何安全风险和建议
- 禁止直接搜索敏感操作的关键字，一定要使用数据流自顶向下的方法进行分析
- 如果代码读写其他文件或者执行脚本，要在项目内查找文件读取内容进一步分析
 （注意：如果从代码能明确的文件名，就不要尝试读其他的文件了。不要在项目目录外查找）。如果source点会作为脚本的参数、或者写入文件。输出要体现
- sink点无敏感操作（比如用于记录日志），不体现在输出报告里。
- 重要：不要读写执行当前目录之外的文件 
- 输出保存在当前目录的reports.json文件中 
- sink点一定是在当前项目找不到代码实现的
- 如果source参数包含多个字段的参数，可以考虑分字段体现，比如arg.name
- 忽略非空的校验

输出报告格式：                                                                                                                                                                                                                                                                                                                          
{          
    "sources": [
      {                                                                                                                                                                                           
        "id": 1,
        "location": {                                                                                                                                                                             
          "file": "src/main/java/com/example/controller/FileController.java",
          "line": 45,
          "class": "FileController",
          "method": "download(HttpServletRequest)"
        },
        "parameterName": "fileName",
        "parameter": "@RequestParam String fileName",
        "from": "请求查询参数 \"fileName\"",
        "sinks": [
          {
            "location": {
              "file": "src/main/java/com/example/service/FileService.java",
              "line": 18,
              "class": "FileService",
              "method": "readFile(String fileName)"
            },
            "operation": "Files.readString(Paths.get(getUploadDir(), fileName))",
            "parameter": "fileName",
            "propagationChain": [
              {
                "location": {
                  "file": "src/main/java/com/example/controller/FileController.java",
                  "line": 50,
                  "class": "FileController",
                  "method": "download(HttpServletRequest)"
                },
                "description": "调用 userService.getUserInfo(fileName)",
                "parameter": "fileName"
              },
              {
                "location": {
                  "file": "src/main/java/com/example/service/UserService.java",
                  "line": 22,
                  "class": "UserService",
                  "method": "getUserInfo(String username)"
                },
                "description": "调用 fileService.readFile(username)",
                "parameter": "username"
              }
            ],
            "validationsBeforeSink": [
              {
                "location": {
                  "file": "src/main/java/com/example/controller/FileController.java",
                  "line": 45
                },
                "code": "@Pattern(regexp = \"^[a-zA-Z0-9_.-]+$\")",
                "description": "仅允许字母、数字、点、下划线、短横线",
                "type": "正则校验",
                "allowedSpecialChars": ["_", ".", "-"]
              }
            ]
          }
        ]
      }
    ]
}


